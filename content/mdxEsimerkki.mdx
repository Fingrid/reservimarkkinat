# MDX Laajennos

import { Blockquote } from "@app/mdx-components/Blockquote.tsx";

<Blockquote>Tämä on lainaus</Blockquote>

export function MyComponent() {
  const array = [1, 2, 3, 4, 5];
  return (
    <div>
      <h1>My Component tulostaa:</h1>
      { array.map((item) => { return <span>[{item}]</span>}) }
      <p>Ja tässä on vielä jotain tekstiä / My Component OUT</p>
    </div>)
}

<MyComponent />
